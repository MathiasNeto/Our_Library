spring:
  jpa:
    database: POSTGRESQL
    show-sql: false
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5432/OurLibrary
    username: postgres
    password: caio1106
    driverClassName: org.postgresql.Driver
  sql:
    init:
      platform: postgres